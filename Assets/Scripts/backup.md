# Timer.cs -> On Trigger Enter
---
    // if ((other.gameObject.name == "XR Origin (XR Rig)" || 
    //      other.transform.IsChildOf(GameObject.Find("XR Origin (XR Rig)").transform)) && !timerRunning)
    // {
    //     StartTimer();
    // }


Porte coordonées : 0.5896629